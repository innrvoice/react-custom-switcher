import { create } from '@storybook/theming';

export default create({
  base: 'dark',
  brandTitle: 'react-custom-switcher',
  fontBase: 'Roboto, "Open Sans", sans-serif',
  fontCode: 'monospace',
});
